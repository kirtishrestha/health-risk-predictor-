"""Placeholder for training and persisting health risk models."""

from pathlib import Path


def train_models(training_data_path: Path, models_dir: Path) -> None:
    """Train machine learning models and save them to disk.

    Args:
        training_data_path: Path to the prepared training dataset.
        models_dir: Directory where trained model artifacts will be stored.
    """
    # Model training logic will be implemented later.
    del training_data_path, models_dir
